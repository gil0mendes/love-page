/**
 * @type {import('vite').UserConfig}
 */
const config = {
  build: {
    rollupOptions: {
      output: {
        // assetFileNames: "[name].[ext]",
      },
    },
  },
};

export default config;
